# Database scaffold

This folder is scaffold-only for Phase 3.

It exists to reserve the eventual Alembic/database layout, but it does not yet
define models, revisions, engines, connection strings, or runtime DB wiring.
Those decisions remain pending the later implementation phases.
