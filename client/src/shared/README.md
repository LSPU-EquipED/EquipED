# Shared frontend scaffold

Place only code that is genuinely reused by 2+ features here.

- `components/` for future generic UI primitives
- `hooks/` for future generic hooks
- `types/` for future app-wide shared types

Keep this layer sparse until real reuse is proven.
