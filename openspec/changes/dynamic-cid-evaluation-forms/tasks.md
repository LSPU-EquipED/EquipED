## 1. Pure Form Contract, Strategy Schemas & Capability Manifests

- [x] 1.1 Implement pure DB-free form models in `server/modules/rubrics/contracts.py`: immutable `FormDefinition`, `DomainDefinition`, `CriterionDefinition`, `ValidationReport`, canonical domain/criterion sorting, string/array bounds, and strict discriminated union for strategy configs (`LlmRubricGuidanceConfig` with bounded guidance and optional 1..4 level descriptors, `CountBandConfig` supporting `minimum_count` and `maximum_count` modes, `RatioBandConfig` supporting `coverage_percentage` and `absolute_difference` modes with optional short-sample override, `CurriculumAlignmentConfig`); verify with `uv run --project server pytest server/tests/rubrics/test_contracts.py`
- [x] 1.2 Implement pure agent capability manifests in `server/modules/rubrics/manifests.py` (`SME_MANIFEST_V1`, `GAD_MANIFEST_V1`, `ITSO_MANIFEST_V1`, `COORDINATOR_MANIFEST_V1`) and pure `validate_form(form, manifest)` validation function enforcing criteria counts, supported strategies, measurement shapes, and prompt character-budget estimates using the existing per-agent settings; verify with `uv run --project server pytest server/tests/rubrics/test_manifests.py`
- [x] 1.3 Implement pure strategy computation and validation functions in `server/modules/rubrics/strategies/` for count bands, ratio bands, and LLM guidance score normalization, guaranteeing strict 1..4 score coverage and monotonic thresholds; verify with `uv run --project server pytest server/tests/rubrics/test_strategies.py`
- [x] 1.4 Enforce case-insensitive global criterion-code uniqueness and verify that every SME, GAD, and ITSO criterion maps to a bounded adapter-v1 measurement shape selected by strategy/mode rather than by code; add contract regressions for novel supported codes and unsupported measurement shapes

## 2. Data Layer, Forward Migration & Lifecycle Repository

- [x] 2.1 Create forward database migration: migrate `rubric_sets.status='active'` to `published`, add CHECK constraint `status IN ('draft', 'published', 'retired')`, add partial unique index for one draft per agent, add composite unique and foreign keys, create `rubric_agent_activations` (with nullable `updated_by` for system seed) and `evaluation_form_snapshots` (with `UNIQUE(evaluation_id, agent_id)`), add nullable `agent_results.form_snapshot_id`, backfill valid strategy configs using exact frozen schemas for all existing criteria (SME `minimum_count` and coverage-ratio bands with OP-01 short-sample config; GAD-01 `maximum_count` thresholds 0/1/3, GAD-03/04/05 `maximum_count` thresholds 0/2/5, and GAD-02 absolute-difference ratio thresholds 2/5/10; ITSO guidance), mark Coordinator v1 as retired, and seed and activate published Coordinator Revision 2 with A-05 `curriculum_alignment`; ensure downgrade explicitly and unconditionally refuses execution; verify migration and strategy backfill assertions on an isolated temporary PostgreSQL test DB with `uv run --project server pytest server/tests/rubrics/test_migrations.py`
- [x] 2.2 Checkpoint for shared Neon DB migration execution: separate post-implementation deployment checkpoint requiring explicit manual administrator approval before executing migration against shared environment (outside implementation critical path)
- [x] 2.3 Update bootstrap/seed scripts to refuse destructive overwrites of published admin revisions; verify with `uv run --project server pytest server/tests/rubrics/test_bootstrap_refusal.py`
- [x] 2.4 Update SQLAlchemy models and repository queries in `server/modules/rubrics/` for `RubricSet`, `RubricDomain`, `RubricCriterion`, `RubricAgentActivation`, and `EvaluationFormSnapshot`; implement transaction-safe shared locking primitives (`FOR UPDATE` on draft mutations/reorders, activation row followed by affected revision rows on activate/retire); verify revision immutability, draft uniqueness, retirement rules, same-agent composite FK integrity, and atomic activation with `uv run --project server pytest server/tests/rubrics/test_models.py`

## 3. Snapshot Service & Result Persistence Foundation

- [x] 3.1 Implement orchestrator form snapshot resolver and binding service in `server/modules/rubrics/` generating recursively immutable frozen DTOs (`EvaluationFormSnapshotDTO`) with deterministic UTF-8 JSON SHA-256 canonical hashing (sorted keys covering `evaluation_id`, `agent_id`, `rubric_set_id`, `adapter_key`, `adapter_version`) and persisting `evaluation_form_snapshots` in a single main-thread transaction coordinated with the job row lock and `EVALUATING` transition; verify atomic pre-dispatch resolution, column-to-payload equality, failure-closed precedence on missing/partial forms, and hash tamper detection with `uv run --project server pytest server/tests/evaluations/test_snapshot_resolution.py`
- [x] 3.2 After the minimum Coordinator teardown in task 4.2, update `persist_agent_outputs` to set `form_snapshot_id` on `AgentResult` for new evaluations, verify matching evaluation/agent identity against the referenced snapshot, verify canonical hash (including `adapter_key` and `adapter_version`), verify column-to-payload equality, and validate returned `CriterionScore.criterion_id` values as the exact snapshot `criterion_code` set; verify persistence integrity with `uv run --project server pytest server/tests/evaluations/test_persistence_integrity.py`

## 4. Agent Adapter Conversions & Worker Isolation Dispatch

- [x] 4.1 Update SME engine to consume the in-memory snapshot, generate bounded measurement extraction schemas from strategy/mode for seeded and newly authored criterion codes, and execute `llm_rubric_guidance`/`count_band`/`ratio_band` configurations (including OP-01 short-sample override); verify Revision-1 parity plus add/remove/reorder behavior with the existing SME suites and a focused snapshot-adapter test
- [x] 4.2 Before enabling strict task 3.2 persistence, decouple Coordinator from SME scores: delete `server/modules/agents/coordinator/reconciliation.py`, remove `EvaluationOrchestrator._reconcile_coordinator_result` and call sites, persist Coordinator output directly, and evaluate curriculum alignment against its own form snapshot Revision 2; update synthesis to calculate Coordinator contribution directly from snapshot criteria; verify Coordinator scoring and synthesis tests with `uv run --project server pytest server/tests/agents/coordinator/test_scoring.py`
- [x] 4.3 Update GAD combined extraction to generate ordered fact sections from in-memory snapshot strategies (`count_band` for grounded adverse-instance lists and `ratio_band` with absolute difference for paired female/male counts) for seeded and newly authored criterion codes, and remove broad `except Exception` fallback and autouse test fixtures; verify Revision-1 parity, add/remove/reorder behavior, and failure propagation with the existing GAD suites and a focused snapshot-adapter test
- [x] 4.4 Update ITSO context assembly, prompt, response schema, title derivation, and validation to use ordered `llm_rubric_guidance` criteria from the in-memory snapshot—including newly authored codes—without worker DB queries; verify Revision-1 parity and add/remove/reorder behavior with the existing ITSO suites and a focused snapshot-adapter test
- [x] 4.5 Update evaluation runner, supervisor, and recovery drainer to pass frozen DTOs to workers, reject partial/mismatched snapshot sets, and reuse existing snapshots on retry without querying active pointers; verify worker DB isolation and concurrency safety with `uv run --project server pytest server/tests/evaluations/test_worker_isolation.py`
- [x] 4.6 Council remediations: enforce Coordinator bounded pre-trimmed duplicate-normalization-unique exact codepoint substring objective extraction with exact substring claim demotion; enforce ITSO packed-chunk substring grounding, omission-marker validation, and zero prompt/raw-response persistence; verify criterion-agnostic GAD/ITSO prompt framing; and apply local LLM byte-capped response reads with 32768-token/3600s limits; verify with focused agent test suites

## 5. Admin API & Form Lifecycle Endpoints

- [x] 5.1 Implement forward migration and model support for `evaluation_jobs.is_pre_snapshot_legacy BOOLEAN NOT NULL DEFAULT FALSE`, backfilling `TRUE` only for coherent historical jobs with persisted `AgentResult` rows, zero `evaluation_form_snapshots`, and all NULL `AgentResult.form_snapshot_id` values (leaving new/nonterminal/queued jobs `FALSE` and refusing downgrade), and update snapshot resolution and supervisor preparation to support both standard active resolution entering `EVALUATING` and precreated snapshot reuse; verify with `uv run --project server pytest server/tests/evaluations/test_legacy_marker.py server/tests/evaluations/test_snapshot_resolution.py`
- [x] 5.2 Implement admin draft lifecycle with shared locking protocol (parent row locks on draft edits/reorder/publish, and activation row followed by affected revision rows in deterministic order on activate/retire), ordering-only bulk reorder (`POST /api/v1/admin/rubrics/{rubric_set_id}/reorder` requiring every criterion to retain its existing domain without reparenting and rejecting missing/duplicate/foreign IDs), capability manifest validation, publication, retirement, and activation rollback endpoints under `/api/v1/admin/rubrics/`; verify RBAC (admin-only), draft uniqueness, non-null actor requirements, deadlock prevention, and immutable conflict responses (HTTP 409) with `uv run --project server pytest server/tests/admin/test_rubrics_api.py`
- [x] 5.3 Update evaluation result endpoints and model validation endpoints: return allowlisted presentation DTOs for authenticated owner-scoped faculty evaluation views (omitting strategy_config, scoring_rule/guidance, raw prompt/response, and unrestricted provenance), return exact active revision and criterion UUID/definitions for partial and full Model Validation preparation catalog, validate echoed active published revisions and exact criterion UUID sets under activation-first/revision locks upon benchmark submission, and persist standard precreated `evaluation_form_snapshots` in the benchmark submission transaction; return `Legacy — form snapshot unavailable` only for evaluations explicitly marked with `is_pre_snapshot_legacy=True`; verify API contracts, partial/full Model Validation workflows, precreated snapshot reuse without activation rereads, and RBAC masking with `uv run --project server pytest server/tests/evaluations/test_evaluations_api.py` and `uv run --project server pytest server/tests/admin/test_model_validation_api.py`

## 6. Admin UI Rubric Editor & Downstream Views

- [x] 6.1 Extend `client/src/features/admin/rubric-editor` to support single-draft authoring, atomic bulk reordering, typed strategy parameter configuration, adapter validation checks, publication, retirement, and activation rollback; verify with `pnpm --dir client test src/features/admin/rubric-editor`
- [x] 6.2 Update evaluation detail views, monitoring matrix, and model validation forms to display snapshot-bound criteria and legacy notices; verify with `pnpm --dir client test`

## 7. Verification, Static Checks & Council Code Review

- [x] 7.1 Run focused writer formatting and lint checks on changed Python files: `uv run --project server ruff check <changed-files>` and `uv run --project server ruff format --check <changed-files>`
- [x] 7.2 Run focused writer checks on changed client files with Vitest and ESLint
- [x] 7.3 Execute three-seat council review covering correctness, performance, and security; execute targeted seat reruns for any remediated code
- [x] 7.4 Execute parent final integrated validation across backend, client, OpenSpec, Alembic, and git diff:
  - Backend full suite: `uv run --project server pytest server/tests`
  - Client full suite, typecheck and build: `pnpm --dir client test`, `pnpm --dir client exec tsc --noEmit`, and `pnpm --dir client build`
  - Alembic single head check: `uv run --project server alembic heads`
  - Strict OpenSpec validation: `openspec validate dynamic-cid-evaluation-forms --strict`
  - Clean git diff check: `git diff --check`
